package it.polimi.ingsw;

import it.polimi.ingsw.Server.RemoteView.RemoteView;
import org.junit.jupiter.api.Test;

public class VisitorTest {
/*
    @Test
    void visitorTest() {
        EventRecived eventRecived = new EventNewGameRequest(3, false);

        EventRecived eventRecivedMessage = (EventRecived) eventRecived;
        RemoteView remoteView = new RemoteView();
        Visitor visitor = new EventVisitor(remoteView);

        eventRecivedMessage.accept(visitor);
    }

 */
}
